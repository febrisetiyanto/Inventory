import 'package:flutter/material.dart';

class Converse extends StatelessWidget {
  const Converse({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("converse")),
      body: const Center(child: Text("Halaman converse")),
    );
  }
}
