import 'package:flutter/material.dart';

class Puma extends StatelessWidget {
  const Puma({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("puma")),
      body: const Center(child: Text("Halaman puma")),
    );
  }
}
